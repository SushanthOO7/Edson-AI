class KnowledgeSearch:
    async def search_customer_safe_knowledge(self, query: str, *, limit: int = 5) -> list[dict]:
        raise NotImplementedError("Knowledge search is planned for the chatbot/RAG phase.")
